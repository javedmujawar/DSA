//Capitalize the first letter of every word
function capitalizeWords(str) {
   return str.split(" ").map((word,_)=>{
     if(word== "") return " ";
     return word[0].toUpperCase() + word.slice(1);
   }).join(" ")
}

console.log(capitalizeWords("javascript is powerful"));